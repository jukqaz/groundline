# Model and effort routing

Let Codex choose its recommended model and effort by default. Override them only
when the task has a clear need and keep the override scoped to that task. Use
lower effort for bounded mechanical work, higher effort for ambiguous architecture,
security, or difficult review, and verify outcomes rather than inferring quality
from the selected label.
