interface:
  display_name: "Audit Agent History"
  short_description: "Build redacted usage evidence and one weekly candidate"
  default_prompt: "Use $audit-agent-history to build redacted usage evidence and propose a workflow candidate for review without applying changes."
policy:
  allow_implicit_invocation: false
