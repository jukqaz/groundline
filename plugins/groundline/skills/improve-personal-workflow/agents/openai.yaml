interface:
  display_name: "Improve Personal Workflow"
  short_description: "Review model guidance or evaluate a personal workflow trial"
  default_prompt: "Use $improve-personal-workflow to improve the requested guidance; use Insights and a personal trial only when that evidence is part of my request."
policy:
  allow_implicit_invocation: false
