interface:
  display_name: "Package Agent Task"
  short_description: "Create a task packet with context and delegation budgets"
  default_prompt: "Use $package-agent-task to prepare a concise handoff with current evidence, authorization boundaries, remaining proof, and the next action."
policy:
  allow_implicit_invocation: false
