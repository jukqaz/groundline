interface:
  display_name: "Close Live Work"
  short_description: "Close runtime work with live endpoint and smoke evidence"
  default_prompt: "Use $close-live-work to verify the requested live outcome and report evidence without assuming a native Goal or authorizing new deployment."
policy:
  allow_implicit_invocation: true
