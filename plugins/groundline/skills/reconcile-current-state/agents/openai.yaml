interface:
  display_name: "Reconcile Current State"
  short_description: "Recheck repo and runtime state before resuming agent work"
  default_prompt: "Use $reconcile-current-state to confirm relevant current state and continue within the existing task and authorization boundary."
policy:
  allow_implicit_invocation: true
