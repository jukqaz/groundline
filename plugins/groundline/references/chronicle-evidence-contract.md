# Optional redacted boundary counts

GroundLine can combine a user-supplied local aggregate with a Codex audit. The
input must contain counters and time boundaries only. Do not provide prompt text,
response text, titles, identifiers, paths, configuration values, or secrets.

This is an explicit local file operation. GroundLine does not discover, enable,
configure, or modify the producer of that aggregate.
